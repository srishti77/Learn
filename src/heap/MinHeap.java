package heap;

public class MinHeap {
	int array[] = {2, 0, 2, 1, 1, 0};
	public static void main(String s[]) {
		
		/*
		 * If given number is higher than both of its children
		 * If given number is lower than one but higher than other
		 * If given number is equal to or lower than both the number break: try both children
		 */
	}
}
